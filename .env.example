# Server port (defaults to 3000)
PORT=3000

# Google Gemini API key used by the AI Sommelier endpoint.
# If unset, the server falls back to an elegant curated recommendation.
GEMINI_API_KEY=YOUR_API_KEY
